
# Runtime-wide setting for the readiness health endpoint
healthEndpoints:
  readiness: /runtimeReadiness

# Runtime-wide setting for buildpacks
buildpacks:
  - runtimeBuildpack

envs:
  - name: "TEST_RUNTIME_VARIABLE"
    value: "test-runtime"
